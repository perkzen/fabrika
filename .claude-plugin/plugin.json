{
  "name": "fabrika",
  "version": "0.0.2",
  "description": "fabrika's stage skills. Loaded into every `claude -p` run via --plugin-dir, so a stage prompt can name a fabrika:* skill in any target repo."
}

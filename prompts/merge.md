Merging `{{base}}` into this branch for ticket {{identifier}} — {{title}} stopped on conflicts in:

{{files}}

Resolve them with the `fabrika:resolving-merge-conflicts` skill and commit the merge. Stop when `git status` shows no unmerged paths and the merge is committed.

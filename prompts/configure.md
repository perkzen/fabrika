Fill in the repo-specific parts of `.fabrika/config.json` for the repository in
the working directory, with the `fabrika:configure` skill.

Return the structured result only; write no files and change nothing.

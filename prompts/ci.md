{{count}} check(s) failed on the PR for ticket {{identifier}} — {{title}} after commit {{sha}}. The host has already rerun each failed run once.

Fix them with the `fabrika:fix-ci` skill, commit, and stop. The host runs the gate and pushes.

{{failures}}

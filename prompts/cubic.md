An automated reviewer left {{count}} unresolved thread(s) on the PR for ticket {{identifier}} — {{title}}.

For each thread below: check whether the finding is valid (the reviewer's own instructions are included). If it is, fix it and commit. If it is not, dispute it with a specific reason.

Return your decisions as structured output: one entry per thread id, with action "fixed" or "disputed" and the reply to post.

{{threads}}
